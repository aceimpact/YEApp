- if user_signed_in?
  - if Like.find_by(user_id: current_user.id, video_id: video.id)
    = link_to video_like_path(video, current_user.id), method: :delete do
      %span.fa.fa-heart.like-btn-unlike
  - else
    = link_to video_likes_path(video), method: :post do
      %span.fa.fa-heart.like-btn
